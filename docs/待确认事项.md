### A. 产品边界确认



```
1. 当前第一版是 auto_wechat 功能子系统验收版，对吗？
回复：对，还有另外一个子功能系统一同验收
2. 当前是否不做完整主系统？
回复：当前不做完整主系统
3. 当前是否需要支持多客户，还是先支持单客户但预留多客户？
回复：支持单客户，但必须预留多客户
4. 当前是否需要登录和权限？
回复：需要，需要有客户登录之后可以管理自己的子功能系统，需要对接上游的主系统users表和权限
5. 当前客户是否能自己维护销售、关键词、工作时间？
回复：需要客户能自己维护销售、关键词、工作时间
6. 当前前端是客户直接使用，还是内部演示使用？
回复：客户直接使用
备注：主系统为客户使用，子系统功能为客户售卖的服务
```



### B. 未来主系统确认



```
1. 未来主系统是否只负责客户管理、权限管理、功能开通？
回复：暂定，后续二期开发任务
2. 主系统是否需要统一管理多个功能子系统？
回复：需要，主系统需要能管理客户和客户使用子功能系统的权限
3. 主系统是否需要统一登录？
回复：主系统需要统一登录
4. 主系统是否需要统一计费/套餐/额度？
回复：需要预留
5. auto_wechat 是否作为其中一个功能模块被开通？
回复：是的
6. 主系统和 auto_wechat 是否共用数据库？
回复：否
7. 主系统通过什么方式调用 auto_wechat？
回复：主系统对auto_wechat有管理权限，并不能直接调用auto_wechat
```



### C. 高并发确认



```
1. 第一阶段预计客户数量是多少？
回复：预计1个
2. 半年内预计客户数量是多少？
回复：预计6~10
3. 单客户每天大概多少线索？
回复：QPS统一先考虑200
4. 峰值每分钟多少线索？
回复：QPS统一先考虑200
5. 每个客户有几台电脑运行 Local Agent？
回复：目前自考虑单个用户一台
6. 每个客户有几个销售微信？
回复：考虑20个
7. 任务拉取频率是否固定 30 秒？
回复：目前只考虑30s
8. 是否允许错峰拉取？
回复：是
9. 是否需要限制每客户并发任务数？
回复：综合考虑
10. 是否需要限制 LLM 兜底调用次数？
回复：暂不考虑
```



### D. 数据源确认



```
1. 线索真实来源是 douyinapi.misanduo.com，还是巨量广告 webhook？
回复：来自还是巨量广告 webhook
2. auto_wechat 是主动拉取，还是接收 webhook？
回复：自动拉取
3. 30 秒拉取接口是否已经存在？
回复：数据库不存在30秒自动拉取，目前确定为实时更新
4. 拉取接口是否有分页、游标、时间范围？
回复：
5. 每条线索是否有 external_lead_id？
回复：数据源示例：
        {
            "id": 6665,
            "open_id": "_000oIJO8Se6jgWfbTuJ2EjkuysOC-7ud6qB",
            "display_name": "白马流星",
            "avatar_url": "https://p3.douyinpic.com/aweme/720x720/aweme-avatar/tos-cn-i-0813c001_oga06ABCEACiADw6fAAgIOAiOnUxGEzaW1KeC7.jpeg?from=3782654143",
            "phone": null,
            "wechat": null,
            "account_open_id": "_0001R_iQXUz4O6nn9yaIMm_XLsSU_8cUBld",
            "latest_event": "im_enter_direct_msg",
            "latest_message_type": "",
            "conversation_short_id": "@9VxWzqPHW8E4PX2vc4woV87902DpOfiFM5x3qQ+nKFUWbfL160zdRmYqig357zEB8eTMZxCQhmKVgeKPMJrsOA==",
            "server_message_id": "@9VxWzqPHW8E4PX2vc4woV87902DpOfqDP5NxrQmnJ1AVbPD060zdRmYqig357zEBZmk/dA7id6qGAV8DcAlDzw==",
            "latest_scene": "im_enter_direct_msg",
            "first_active_time": "1781252553045",
            "latest_active_time": "1781252553045",
            "lead_status": "pending",
            "assignee": null,
            "remark": null,
            "lead_channel": "企业号",
            "lead_type": "私信",
            "tags": [],
            "last_interaction_record": null
        },
6. 拉取成功后是否需要回写已处理？
回复：拉取成功并且local Agent已接收任务后才回写已处理
7. 数据源是否会重复返回同一线索？
回复：不会重复返回同一线索
```



### E. 回调确认



```
1. 是否真的需要回调数据源？
回复：需要
2. 回调给谁？
回复：回调地址
3. 哪些状态需要回调？
回复：received、invalid、assigned、replied、timeout、reassigned、callback_success、manual_required、failed
4. 超时重新分配是否回调？
回复：是
5. 有效回复是否回调？
回复：是
6. 回调失败 5 次后由谁人工处理？
回复：保存错误信息，开发人员复核处理
```



### F. 客户配置确认



```
1. 客户是否可以配置销售列表？
回复：可以
2. 客户是否可以配置销售微信昵称？
回复：可以
3. 客户是否可以配置关键词？
回复：可以
4. 客户是否可以配置工作时间？
回复：可以
5. 客户是否可以配置超时时间？
回复：可以
6. 客户是否可以配置是否启用 LLM 兜底？
回复：不能，默认启动
7. 客户是否可以配置是否自动重分配？
回复：可以
```



------

### 五、我的结论

你这次补充之后，最关键的调整是：



```
auto_wechat 不再是“接入已有主系统”的副系统；
而是“先独立验收，未来接入上游主系统”的功能子系统。
```



所以当前还需要重点确认：



```
1. 当前验收版是否单客户还是多客户；
回复：验收版为单客户
2. 是否现在就做轻量客户配置；
回复：需要
3. 所有表是否从第一版开始预留 customer_id / tenant_id；
回复：需要
4. 高并发目标数字是多少；
回复：暂时考虑QPS200
5. 线索到底是主动拉取还是 webhook；
回复：主动拉取
6. 回调哪些状态；
7. 未来主系统和 auto_wechat 的接口边界；
回复：auto_wechat做为主系统下游的一个子功能系统
8. Local Agent 并发和任务串行规则；
回复：考虑QPS200
9. LLM 兜底是否第一版启用；
回复：启用
10. 数据保存半年如何分批清理。
回复：你来提供方案
```

下面是还需要你再次确认的问题。我按“必须确认 / 可以稍后确认”分组。

## P0：必须再次确认

1.  **QPS 200 的真实口径**
    请确认：200 QPS 是服务端 API 压测目标，还是线索进入速度？
    我建议定义为：**服务端接口承压预留 200 QPS，Local Agent 不承诺 200 QPS，微信执行按单机串行。** 
2.  **单客户真实线索量**
   考虑单客户一天峰值考虑2000
3.  **主动拉取接口是否已经存在**
    现在确认是主动拉取，但还缺：接口地址、参数、鉴权、分页、游标、时间范围、返回字段、错误码。 
4.  **“实时更新”是什么意思**
   已有实时 API？auto_wechat 从API拉？ 
5.  **拉取成功后回写给谁**
   回调地址
6.  **主系统 users 表如何对接**
    当前主系统未做，但你确认需要对接 users 表和权限。需要确认第一版是本地登录，还是先预留 token 校验，还是马上实现主系统鉴权接口。 
7.  **主系统和 auto_wechat 不共库时，customer_id 如何同步**
    需要确认未来 customer_id 由主系统生成，还是 auto_wechat 先生成，后续再映射。**（待定）** 
8.  **callback_success 是否只作为内部状态**
    不要对外回调 `callback_success`，它应该只放在 `callback_logs.status = success`。 
9.  **LLM 默认启用是否允许系统级限流和熔断**
    客户不能关闭可以，但系统必须能限流、超时、熔断，避免成本和稳定性风险。 **（待定）** 
10.  **Local Agent 串行规则是否最终确认**
     同一台电脑、同一个微信窗口、同一个 agent_client_id，同一时间只允许一个任务执行。 

## P1：建议尽快确认

1.  第一版登录怎么做：本地账号、临时客户账号，还是预留主系统 token？ 
   回复：你来给建议

1.  客户配置页第一版字段有哪些？ 
   回复：你来给建议
2.  销售列表是否需要 Excel 导入？ 
   回复：需要快捷导入
3.  工作时间是客户统一配置，还是每个销售单独配置？ 
   回复：目前只考虑统一配置
4.  超时重分配最多几次？ 
   回复：最多5词
5.  超时重分配是否排除原销售？ 
   回复：排除原销售
6.  销售被停用后，历史跟进线索如何处理？ 
   回复：暂不考虑，客户目前没有提这个需要
7.  销售列表为空时，线索进入什么状态？ 
   回复：待分配状态
8.  回调地址、签名、鉴权、payload 格式由谁提供？ 
   回复：已有接口文档，后面等待解析后填写
9.  回调失败 5 次后，开发人员在哪里处理：后台页面、数据库、日志，还是运维脚本？ 
   回复：目前考虑在后台页面处理

## P2：可以后置，但要预留

1.  未来主系统是否统一套餐、计费、额度？ 
   回复：二期开发，需求未定
2.  多客户上线后是否需要每客户独立回调地址？ 
   回复：二期开发，需求未定
3.  每个客户是否可以绑定多台 Local Agent？ 
   回复：暂不考虑
4.  每个客户是否可以绑定多个抖音/巨量账号？ 
   回复：暂不考虑
5.  是否需要客户级数据导出？ 
   回复：需要
6.  是否需要操作审计日志？ 
   回复：不需要
7.  是否需要主系统停用客户后立即停止 Local Agent 拉任务？ 
   回复：需要
8.  截图文件到底保存 30 天、90 天，还是 180 天？ 
   回复：目前只考虑30天
9.  LLM trace 是否保存完整 prompt/response，还是脱敏保存？ 
   回复：脱敏保存
10.  后续是否需要把 auto_wechat 做成多个子系统中的一个统一模块入口？
    回复：需要

--------------------------------

## P0 必须确认

1. 主动拉取 API 文档
   -  接口地址 
   -  请求方法 
   -  鉴权方式 
   -  请求参数 
   -  分页 / 游标 / 时间范围 
   -  返回字段 
   -  错误码 
   -  限流规则 
   -  空数据返回格式 
2. 回调接口文档
   -  回调地址 
   -  鉴权 / 签名方式 
   -  payload 格式 
   -  成功响应格式 
   -  失败响应格式 
   -  哪些状态必须回调 
   -  是否支持手动重试 
3. 数据源“已处理”回写规则
   -  Local Agent 已接收任务后回写什么状态 
   -  微信发送成功后是否再次回写 
   -  有效回复后是否再次回写 
   -  超时重分配后是否再次回写 
   -  回写使用哪个唯一 ID：`id` / `open_id` / `conversation_short_id` / `server_message_id` 
4. LLM 失败策略
   -  LLM 调用失败时，状态是 `check_failed` 还是 `detected_invalid` 
   -  LLM 超时时间是多少，建议 10～15 秒 
   -  LLM 熔断后是否只使用关键词判断 
5. Excel 销售导入规则
   -  销售姓名是否必填 
   -  微信昵称是否必填 
   -  手机号是否必填 
   -  重复微信昵称是覆盖、跳过，还是报错 
   -  导入失败是否返回错误行号和原因 
6. 数据导出格式
   -  第一版导出 Excel 还是 CSV 
   -  导出范围是否包括：线索、分配记录、通知任务、回复检测、超时记录、回调失败记录、人工处理记录 
7. 回调失败后台处理
   -  是否支持手动重试 
   -  是否支持标记已处理 
   -  是否支持标记忽略 
8. 非工作时间策略
   -  默认是否采用 `delay_assign`：先入库，等工作时间再自动分配 
   -  非工作时间是否允许人工立即分配 
9. Local Agent 离线策略
   -  心跳间隔是否为 30 秒 
   -  90 秒无心跳是否判定离线 
   -  离线期间任务是否继续排队 
   -  恢复在线后是否自动继续拉任务 

## P1 建议确认

1. 前端第一版页面范围
   -  登录页 
   -  首页概览 
   -  线索列表 
   -  线索详情 
   -  销售管理 
   -  销售导入 
   -  关键词配置 
   -  工作时间配置 
   -  超时 / 重分配配置 
   -  Local Agent 状态 
   -  微信任务列表 
   -  回复检测列表 
   -  超时列表 
   -  回调失败列表 
   -  人工处理页 
   -  数据导出页 
2. 客户停用后的处理
   -  是否立即禁止登录 
   -  是否立即停止拉取线索 
   -  是否立即停止 Local Agent 拉任务 
   -  已创建但未执行任务如何处理 
3. 自动重分配边界
   -  最多重分配 5 次后进入 `manual_required`，是否确认 
   -  所有销售都超时后是否进入 `manual_required` 
   -  原销售是否只排除本轮，还是后续所有重分配都排除 
4. 待分配状态处理
   -  销售列表为空时进入 `pending_assign` 
   -  客户补充销售后是否自动继续分配 
   -  是否需要人工点击“重新分配” 
5. 截图保存内容
   -  只保存失败截图，还是成功 / 失败都保存 
   -  截图是否允许客户前端查看 
   -  截图 30 天后是否直接删除 
6. LLM trace 脱敏规则
   -  手机号是否脱敏 
   -  微信号是否脱敏 
   -  open_id 是否脱敏 
   -  聊天文本是否部分脱敏还是完整保存 

## P2 后置确认

1.  多客户是否每个客户独立回调地址 
2.  多客户是否每个客户独立 LLM 配置 
3.  每客户是否支持多台 Local Agent 
4.  每客户是否支持多个巨量 / 抖音账号 
5.  未来主系统是否统一套餐、计费、额度 
6.  多子系统统一入口由主系统做，还是 auto_wechat 也需要预留入口菜单 
7.  销售停用后的历史线索迁移规则