# auto_wechat / 小高AI微信助手 最终 PRD 冻结版

版本：P0-DOC-PRD-1
状态：最终 PRD 冻结与项目上下文同步
范围：只定义产品边界、业务规则、对接边界和后续待设计内容，不作为技术方案或数据库迁移方案。

------

## 1. 项目总定位

auto_wechat / 小高AI微信助手属于 NewCarProject 外部客户系统下的一组商户可售卖子功能系统。

整体结构：

```text
NewCarProject
  ↓
外部客户系统 / React 商户端
  ↓
多个可售卖子功能系统
```

NewCarProject 负责商户、账号、权限、菜单、套餐、消耗管理。外部客户系统是商户入口。

子功能系统包括：

1. 抖音AI小高客服
2. AI小高线索
3. 小高AI微信助手
4. AI小高剪辑
5. 小高素材库
6. 小高算力

其中小高算力不是子功能系统，是商户查看套餐和消耗的展示能力。

------

## 2. 当前重点链路

当前重点建设链路：

```text
AI小高线索 → 小高AI微信助手
```

AI小高线索负责：

1. 抖音扫码鉴权。
2. 获取抖音私信。
3. 识别线索来源。
4. 记录原始线索事件。
5. 从用户私信纯文本中提取手机号 / 微信号。
6. 生成有效线索。

小高AI微信助手负责：

1. 获取 AI小高线索提供的有效线索。
2. 分配销售。
3. 创建微信通知任务。
4. 调用 Local Agent 操作客户本地微信。
5. 通知销售。
6. 检测销售是否回复。
7. 判断已回复 / 超时未回复。
8. 支持超时重分配。
9. 支持失败记录。
10. 支持人工处理。
11. 支持 Excel 数据导出。

------

## 3. AI小高剪辑与巨量一键过审边界

AI小高剪辑是独立售卖的子功能系统。

巨量一键过审属于 AI小高剪辑，不属于小高AI微信助手。

巨量一键过审需要巨量服务扫码鉴权。

当前 douyinAPI 中已有巨量一键过审和巨量扫码鉴权 demo 代码，但未经过完整验证。

巨量一键过审后续单独出 PRD，不混入 auto_wechat 第一版。

------

## 4. douyinAPI 定位

douyinAPI 当前定位为：

```text
demo / 参考实现 / 历史代码沉淀
```

douyinAPI 中已有但未完整验证的能力包括：

1. 抖音扫码鉴权相关代码。
2. 抖音私信 / 线索相关代码。
3. 巨量一键过审相关代码。
4. 巨量服务扫码鉴权相关代码。

后续产品化时，不应直接把 douyinAPI 作为长期正式依赖，而应按子功能边界拆分、验证和迁移。

对应关系：

```text
抖音扫码鉴权、抖音私信线索 → AI小高线索
巨量扫码鉴权、巨量一键过审 → AI小高剪辑
线索分配、微信通知、回复检测 → 小高AI微信助手
```

------

## 5. 服务拆分与隔离要求

AI小高线索、小高AI微信助手、AI小高剪辑等子功能服务需要具备独立运行能力。

要求：

1. 独立启动。
2. 独立部署预留。
3. 独立配置。
4. 独立健康检查。
5. 独立日志。
6. 独立异常处理。
7. 一个服务异常不影响另一个服务已有功能。
8. 后续服务器资源不足时，可以拆分到不同服务器部署。
9. 第一版暂不设计智能路由。
10. 服务地址、端口、健康检查地址必须可配置，不能写死。

------

## 6. Webhook 线索接入规则

第一版正式验收链路为：

```text
webhook 直收
```

正式地址继续使用：

```text
callback.misanduo.com/webhook/douyin
```

Webhook 验签使用《抖音私信能力对外 OpenApi》文档里的签名规则：

```text
signature = sha256Hex(SECRET_KEY + body + "-" + timestamp)
```

Header：

```text
Authorization: signature
X-Auth-Timestamp: timestamp
```

规则：

1. `body` 为请求体原始字符串。
2. `timestamp` 为秒级时间戳。
3. `SECRET_KEY` 第一版按客户 / 商户维度配置。
4. 后续如果每个抖音账号需要不同 `SECRET_KEY`，再扩展到账号维度。

返回规则：

1. 成功接收：HTTP 200
2. 重复事件：HTTP 200
3. 非线索事件：HTTP 200
4. 无效线索：HTTP 200
5. 请求格式错误：HTTP 400
6. 签名失败：HTTP 401
7. 过期请求：HTTP 401
8. 系统异常：HTTP 500

对外成功响应建议：

```json
{
  "code": 0,
  "msg": "success"
}
```

------

## 7. 原始事件与有效线索规则

所有 webhook 事件进入：

```text
lead_source_events
```

有效线索进入：

```text
douyin_leads
```

有效线索判断规则：

1. 用户留下资料时，联系方式通常出现在用户发出的私信纯文本中。
2. 系统接收私信 webhook 后，解析用户发出的私信文本内容。
3. 如果文本中能提取到手机号或微信号，则创建 / 更新有效线索。
4. 手机号或微信号任一存在，即视为有效线索。
5. 有效线索进入 `douyin_leads`，并参与销售分配。
6. 如果文本中无法提取手机号或微信号，则只记录原始事件。
7. 无联系方式的事件进入 `lead_source_events`，不进入有效线索分配。
8. invalid 进入前端列表。
9. invalid 参与数据导出。
10. invalid 不需要回调。
11. 第一版不依赖顶层 `phone` / `wechat` 字段。
12. 第一版不依赖 `retain_consult_card` 留资卡片。
13. 第一版不接入 LLM。
14. 第一版采用正则 / 规则提取联系方式。

------

## 8. 联系方式提取规则

手机号识别：

```text
中国大陆 11 位手机号
```

微信号识别以下关键词后的账号：

```text
微信
wx
vx
v
加我
```

多联系方式处理：

1. 一个消息中如果出现多个手机号 / 微信号，全部保存。
2. 主字段取第一个。
3. 原始文本完整保存。
4. 提取结果保存。
5. 提取失败原因保存。

建议字段：

```text
raw_message_text
extracted_phone
extracted_wechat
all_extracted_contacts
contact_extract_status
contact_extract_reason
```

------

## 9. 唯一键与幂等

优先使用数据源 `id` 作为：

```text
external_lead_id
```

如果 `id` 缺失，使用：

```text
open_id + account_open_id
```

辅助字段：

1. `conversation_short_id`：会话级辅助去重。
2. `server_message_id`：事件级幂等字段。
3. `event_key`：webhook 事件幂等字段。

重复触发规则：

1. 同一 `open_id + account_open_id` 多次触发时，更新原线索。
2. 同一用户不同会话，视为同一用户线索更新。
3. 重复事件不重复创建线索。
4. 重复事件需要返回成功。
5. 重复事件需要记录幂等命中结果。

------

## 10. 状态规则

`douyin_leads.status` 建议包括：

1. received：已接收
2. invalid：无效线索
3. delay_assign：非工作时间延迟分配
4. pending_assign：未分配
5. assigned：已分配
6. notified：已通知销售
7. waiting_reply：等待回复
8. replied：已回复
9. timeout：超时未回复
10. reassigned：已重新分配
11. manual_required：需要人工处理
12. failed：失败
13. closed：人工关闭

对外状态只包括：

1. 未分配
2. 已分配
3. 已回复
4. 超时未回复

内外状态映射：

```text
pending_assign → 未分配
delay_assign → 未分配
reassigned → 未分配
assigned → 已分配
replied → 已回复
timeout → 超时未回复
```

以下状态不对外回调：

```text
received
invalid
agent_pulled
notified
send_failed
manual_required
failed
closed
callback_success
```

`callback_success` 仅作为内部 `callback_logs.status = success`，不作为对外业务状态。

------

## 11. NewCarProject 登录与权限预留

NewCarProject 跳转 auto_wechat 使用：

```text
token + cookie
```

auto_wechat 需要支持识别 token 和 cookie。

当前 NewCarProject 同事暂时不能继续推进 token / cookie / roles / merchant_id 的具体字段结构。因此第一版由 auto_wechat 先做预留设计。

必须标记为后续待确认项：

```text
NewCarProject token / cookie / roles / merchant_id 的具体字段结构，后续需要与 NewCarProject 同事确认。
```

customer_id 映射规则：

1. auto_wechat 本地生成 `customer_id`。
2. NewCarProject 的商户 ID 保存为 `external_customer_id`。
3. 后续 NewCarProject 正式接入时，通过 `external_customer_id` 建立映射。

角色跳转规则：

1. 商户进入 auto_wechat。
2. 商户不允许跳转到其他子功能。
3. roles 不是商户的用户，由 NewCarProject 跳转到多子功能菜单。
4. auto_wechat 只负责自己的子系统使用权限。

------

## 12. 密码规则

第一版支持修改密码。第一版不支持重置密码。

修改密码规则：

1. 需要输入旧密码。
2. 新密码最少 8 位。
3. 建议数字 + 字母。
4. 修改后强制重新登录。

------

## 13. 销售管理和导入

销售信息至少包括：

1. 微信昵称
2. 销售姓名
3. 手机号
4. 备注
5. 排序
6. 状态

Excel 导入规则：

1. 微信昵称必填。
2. 销售姓名可空。
3. 手机号可空。
4. 备注可空。
5. 排序自动生成。
6. 重复微信昵称时覆盖。
7. 导入失败返回错误行号和原因。
8. 支持部分成功。
9. 提供模板下载。

------

## 14. 分配与超时

自动分配规则：

1. 按客户销售列表顺序轮流分配。
2. 自动排序。
3. 避免同一销售连续接收过多线索。
4. 销售列表为空时进入未分配。
5. 非工作时间进入 `delay_assign`。
6. 到工作时间后继续自动分配。

超时重分配规则：

1. 超时时间客户可配置。
2. 默认超时时间为 30 分钟。
3. 最多重分配 5 次。
4. 重分配排除原销售。
5. 超过次数后进入人工处理或失败记录。
6. 超时状态对外映射为“超时未回复”。
7. 重新分配后对外映射为“未分配”。

------

## 15. Local Agent / 小高AI微信助手安全规则

1. 每个客户第一版只考虑一台电脑运行 Local Agent。
2. 不支持客户多台 Local Agent。
3. 不支持多个账号。
4. 同一电脑、同一微信窗口、同一 agent_client_id，同一时间只允许执行一个任务。
5. 发送任务和检测任务必须互斥。
6. poll-and-execute 与 poll-and-detect 必须互斥。
7. 忙碌时返回 agent_busy。
8. 微信异常时停止操作并回写失败原因。
9. 未确认联系人时禁止发送。
10. 微信窗口不可用时禁止继续操作。
11. 搜索框焦点未确认时禁止粘贴。
12. 禁止并发操作微信。
13. 高风险操作必须有日志。
14. 失败必须回写。

exe 名称统一为：

```text
小高AI微信助手
```

------

## 16. 回复检测

第一版不接入大模型。

回复检测采用：

1. 关键词判断。
2. 规则判断。

有效回复关键词由客户配置。

示例：

```text
收到
已添加
已添加微信
已联系
正在跟进
已跟进
```

关键词配置提示：

1. 不要与线索模板内容重复。
2. 不要设置过短关键词。
3. 不要设置过宽泛关键词。
4. 应配置明确表达已处理意图的关键词。

------

## 17. 人工处理

第一版人工处理主要面向失败状态。

允许：

1. 人工重新分配。
2. 人工补录销售回复。
3. 人工关闭线索。

人工处理后：

1. 重新分配后进入未分配或分配流程。
2. 补录销售回复后可进入已回复。
3. 人工关闭线索后进入 `closed`。
4. `closed` 不对外回调。
5. 第一版 `closed` 后不允许恢复。

------

## 18. 数据导出

第一版导出格式：

```text
Excel
```

导出要求：

1. 支持按时间范围导出。
2. 导出线索列表。
3. 导出分配记录。
4. 导出微信通知任务。
5. 导出回复检测结果。
6. 导出超时记录。
7. 导出回调失败记录。
8. 导出人工处理记录。
9. 第一版导出不脱敏。

------

## 19. 数据保存与清理

业务数据保存：

```text
180 天
```

包括：

1. 原始事件。
2. 有效线索。
3. 分配记录。
4. 微信任务。
5. 回复检测。
6. 超时记录。
7. 回调失败记录。
8. 人工处理记录。

截图规则：

1. 截图不保存。
2. 截图不入库。

数据归档：

1. 第一版不做数据归档。
2. 客户如需历史数据，由客户自行下载。

------

## 20. 性能口径

服务端口径：

1. 服务端 API 和数据库层面预留 200 QPS。
2. 单客户峰值按 2000 条线索 / 天考虑。
3. 查询接口需要分页。
4. 导出接口需要按时间范围。
5. webhook 接收需要幂等。
6. 状态更新需要幂等。
7. 任务回写需要幂等。

Local Agent 口径：

1. Local Agent 不承诺 200 QPS。
2. Local Agent 必须串行执行微信任务。

------

## 21. 第一版明确不做

1. 不把 douyinAPI 作为正式生产长期依赖。
2. 不把 AI小高线索、小高AI微信助手、AI小高剪辑混成一个服务。
3. 不把巨量一键过审放入小高AI微信助手。
4. 不接入 LLM。
5. 不保存截图。
6. 不做数据归档。
7. 不支持客户多台 Local Agent。
8. 不支持多个微信账号。
9. 不支持重置密码。
10. 不设计智能路由。

------

## 22. 后续待确认与待设计内容

后续待确认：

1. NewCarProject token / cookie / roles / merchant_id 的具体字段结构。
2. Webhook 请求体更多真实样例。
3. 每个抖音账号是否需要独立 `SECRET_KEY`。
4. 状态同步接口格式。
5. 服务器部署和域名配置说明。

后续待单独设计：

1. AI小高线索 PRD。
2. AI小高剪辑 / 巨量一键过审 PRD。
3. 架构设计文档。
4. 服务拆分设计文档。
5. 数据模型设计文档。
6. 接口契约文档。
7. 测试验收计划。
