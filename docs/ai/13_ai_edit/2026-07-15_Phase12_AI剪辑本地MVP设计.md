# Phase 12 AI剪辑本地 MVP 设计

## 1. 文档状态

| 项目 | 内容 |
|---|---|
| 冻结日期 | 2026-07-15 |
| 目标阶段 | Phase 12 |
| 产品模块 | 小高素材库、AI 小高剪辑 |
| 设计状态 | 基础 MVP 已落地；Task 12-1/12-2 已提交，检查点 A 为 `CHECKPOINT_A_BLOCKED` |
| 生产验证 | Phase 13 完成后统一在宝塔验证 |
| 来源项目 | `E:\work\project\auto_edit`、`E:\work\project\BrollStudio_空镜素材增稳分析入库工具_交付包_20260714_114227` |

本设计替代一期主计划中“Phase 12 只做迁入预留”的旧结论。Phase 12 基础本地/模拟 MVP 与 Task 11 测试 EXE 已落地；2026-07-16 批准的 Task 12 素材库真实闭环增强设计见 `docs/superpowers/specs/2026-07-16-phase12-task12-ai-edit-material-library-closed-loop-design.md`。两个来源项目只作为源码迁入来源，正式运行不得依赖外部仓库路径或 CLI。

## 2. 产品目标

商户运营人员在安装小高AI微信助手的同一台 Windows 电脑上完成：

```text
导入汽车口播素材
  -> 素材分析与可选增稳
  -> 选择平台剪辑模板
  -> AI 生成保守剪辑计划
  -> 轻量人工调整
  -> 720P 预览
  -> 1080P 成片
  -> 本地下载或主动上传云端
```

一期只服务抖音汽车营销短视频：竖屏 `9:16`、目标时长 `15-60` 秒、中文字幕。成片不直接上传或发布到抖音。

## 3. 用户、权限与隔离

- 商户运营人员是主要用户。
- `auto_wechat:ai_edit` 同时控制“小高素材库”和“AI 小高剪辑”。
- 同商户且有权限的用户共享素材、任务和成片，并记录创建人和操作审计。
- 商户私有素材严格隔离；禁止商户间共享。
- 超管只维护平台公共素材、剪辑模板、配置和审计，不代替商户执行日常剪辑。
- `merchant_id` 只来自 9000 可信登录上下文或 Local Agent 鉴权映射，前端不得自报。

## 4. 一期非目标

- 不建设完整多轨时间线、关键帧、滤镜和专业音频编辑器。
- 不做 ROI 点选、框选或涂抹增稳。
- 不做远程设备控制和多设备管理。
- 不做商户间素材共享。
- 不做自动云同步和永久草稿版本历史。
- 不接抖音草稿上传、自动发布或网络素材抓取。
- 不建设向量语义检索；一期使用结构化标签和关键词。
- 不迁入来源项目的 PySide6、SQLite、本地 JSON 任务库、固定豆包客户端或开发 runner。
- 不调用外部 `auto_edit` 或 BrollStudio 仓库作为运行时依赖。

## 5. 总体架构

```text
React（与小高AI微信助手同机）
  -> 9000：登录、权限、商户 metadata、公共素材、任务、审计
  -> 127.0.0.1:19000：本地导入、预览、队列、进程监管

19000 小高AI微信助手（Python 3.10）
  -> 本地受管素材目录
  -> 默认单任务队列
  -> 启动/取消 ai_edit_worker.exe
  -> 与 9000 通信并转发安全进度

ai_edit_worker.exe（Python 3.11）
  -> ASR、分镜、标签、自动增稳
  -> 候选片段、字幕、BGM、FFmpeg 渲染
  -> 720P 预览、1080P 成片、媒体强校验
```

小高AI微信助手仍是一个产品和一个安装目录，但剪辑能力在独立子进程中运行。剪辑 Worker 缺失或损坏时只禁用 AI剪辑，不影响微信助手启动、轮询或安全门禁。

一期采用“一任务一子进程”。默认并发数为 `1`；只有实测启动和模型加载成本不可接受时，才升级为长驻 Worker。

## 6. 小高素材库

### 6.1 素材范围

- 商户私有素材：视频、图片、音频，默认仅本地保存。
- 平台公共素材：汽车空镜、片头、片尾和授权配乐，由超管维护，商户只读。
- 配乐只来自平台授权公共库或商户私有上传，禁止联网抓取。

### 6.2 本地导入

1. 浏览器支持拖拽、多选视频和选择文件夹；前端逐项把文件流式传给 19000，单个失败不阻断批次。
2. 19000 写临时文件并计算 SHA-256，执行 ffprobe 校验真实媒体和生成低清缩略图，原文件只读且不被覆盖。
3. 9000 按可信商户上下文与 SHA-256 去重并返回规范素材 ID；同商户并发重复导入由数据库唯一约束阻断。
4. 19000 使用规范素材 ID 原子写入受管目录，并向 9000 同步安全 metadata 与缩略图；9000 不保存本机绝对路径。
5. 当前电脑缺少既有同哈希素材时允许补齐本地副本，不重复创建商户素材记录。
6. 19000 的本地清单把素材 ID 映射到受管目录相对路径；批次结束后前端从真实接口恢复逐项状态。

### 6.3 存储状态

```text
local_only
  -> uploading
  -> cloud_available

local_only/cloud_available
  -> local_missing
  -> trashed
  -> purged
```

- 默认 `local_only`；用户单个或批量主动选择后，19000 才把受管文件流式上传到 9000 宝塔受控目录。
- 仅本地素材在其他设备不可预览或剪辑。
- 平台公共素材固定为云端可用，19000 下载后必须复验大小和 SHA-256。
- 上传成功后保留本机副本；云端与本地共享业务 metadata，但不自动双向同步完整媒体。

### 6.4 分层分析

- 导入成功后自动排队执行：媒体探测、缩略图、ASR、分镜、关键帧、稳定性评分和可用区间。
- 媒体解析、语音转写、内容分析、稳定性检测和云端上传分别记录状态、进度、attempt 与稳定失败码；`pending` 不得统一展示为“处理中”。
- 19000 只把必要的低分辨率关键帧、转写和媒体摘要提交给 9000，再由 9000 调 9100 生成描述、分类、标签、高光和可用区间；原始视频不得发送给 9100。
- 分析结果按源哈希、算法版本和模型版本复用；人工确认结果独立保存，重新分析不得覆盖。
- 9100 不可用时保留本地分析结果，内容分析单独失败并可重试，禁止伪造结果。

### 6.5 删除

- 活动任务引用的素材禁止删除。
- 删除后进入 7 天回收站，可恢复。
- 回收期内本地和云端受管副本均保留；永久删除或到期后协调清理两端副本，19000 离线时在下次启动或心跳补偿。
- 安全指纹和审计记录保留，恢复和清理操作必须幂等。

## 7. AI 小高剪辑

### 7.1 任务输入

- 至少一条主素材；业务层不限制素材数量、输入总时长或文件大小。
- 可选空镜、图片和授权配乐。
- 选择平台模板、目标时长和增稳选项。
- 无业务上限不等于无安全门：磁盘、临时空间、并发、阶段超时和重试次数必须可配置。

### 7.2 保守剪辑规则

- 主素材口播和画面保持同步。
- AI 只删除静音、口误、明显重复和无效片段。
- AI 保持主口播时间顺序，不生成原素材中不存在的车辆事实。
- 空镜只覆盖适合补画面的区间，继续使用主素材口播音频。
- AI 可生成标题、字幕和结尾行动引导建议。
- 9100 不可用时明确失败，不启用隐式规则兜底。

### 7.3 处理阶段

```text
preflight
  -> analyze
  -> stabilize_optional
  -> plan
  -> render_preview_720p
  -> review
  -> render_final_1080p
  -> verify
  -> completed
```

增稳只处理用户勾选或系统建议的素材，始终生成衍生文件并默认保留音频。BrollStudio 自动模式当前使用 `-an`，迁入时必须修正，不能静默丢音轨。

### 7.4 轻量调整

- 删除或调整片段顺序。
- 修改片段首尾时间。
- 替换空镜。
- 修改字幕文字、断句和时间范围。
- 开关字幕、配乐和指定素材增稳。

每次重新渲染替换当前草稿；用户确认后冻结成片。只保留当前草稿和已确认成片，不保留永久版本历史。

### 7.5 输出

- `720x1280` 快速预览。
- `1080x1920` 正式成片。
- H.264 视频、AAC 音频，帧率跟随主素材并收敛到常用范围。
- 成片默认保存在本机，用户可下载或主动上传云端。

## 8. 任务状态与恢复

```text
queued -> running -> review_required -> succeeded
queued/running/review_required -> cancel_requested -> cancelled
queued/running -> failed
```

- 19000 默认单任务执行，其余排队。
- 取消必须终止 Worker 和 FFmpeg 进程树。
- 重启时把未完成的 `running` 任务重新入队，复验阶段产物后从最近安全阶段继续。
- 人工重试创建新 attempt，旧进程和旧令牌不能覆盖新结果。
- 进程退出但没有合法结果文件时记为失败，不伪造成功。

每个任务使用独立目录：

```text
ai_edit/jobs/{job_id}/{attempt_id}/
  manifest.json
  input/
  stages/
  output/
  progress.jsonl
  result.json
  diagnostics/
```

任务目录和路径全部由 19000 生成，前端不能提交绝对路径。

## 9. 9100 与算力

- Worker 不直接访问 9100，也不持有模型令牌。
- 19000 经 9000 调用 9100，只发送转写文本、镜头标签、时长和稳定性摘要。
- 9100 输出严格、版本化的剪辑计划 JSON；未知字段、非法区间和越界值必须拒绝。
- 仅 9100 剪辑规划、标题和行动引导计入现有 `compute` 算力。
- 本地 ASR、FFmpeg、分镜和增稳不扣算力。
- 原始模型响应不得进入普通日志、商户 artifact 或前端响应。

## 10. 数据模型方向

复用并扩展现有 `AiEditJob`、`AiEditJobArtifact`，不另建第二套任务表。

新增最少对象：

- `AiEditMaterial`：素材归属、媒体属性、设备、存储和生命周期状态。
- `AiEditMaterialAnalysis`：版本化 ASR、分镜、标签、稳定性和可用区间。
- `AiEditTemplate`：平台模板、剪辑规则和 Prompt 版本。
- `AiEditJobMaterial`：任务素材角色、顺序、固定哈希和使用区间。

扩展任务壳：状态、阶段、进度、设备 ID、attempt、执行令牌哈希、取消/心跳、输入指纹、引擎/模板/模型版本和稳定失败码。

扩展产物壳：本地/云端位置类型、设备 ID、SHA-256、大小、媒体属性、完整性状态和来源产物 ID。外部 API 不返回绝对路径、`storage_key`、执行令牌或 `merchant_id`。

一期直接复用现有 `agent_client_id` 绑定本地素材，不新建多设备管理系统。

## 11. API 边界

9000 负责：

```text
/ai-edit/materials
/ai-edit/templates
/ai-edit/jobs
/ai-edit/jobs/{id}/cancel
/ai-edit/jobs/{id}/retry
/ai-edit/jobs/{id}/plan
/ai-edit/jobs/{id}/confirm
/ai-edit/artifacts/{id}/preview
/ai-edit/artifacts/{id}/download
```

19000 负责：

```text
/agent/ai-edit/materials/import
/agent/ai-edit/materials/{id}/preview
/agent/ai-edit/materials/{id}/analyze
/agent/ai-edit/materials/{id}/stabilize
/agent/ai-edit/jobs/{id}/start
/agent/ai-edit/jobs/{id}/cancel
/agent/ai-edit/status
```

9000 接口统一校验 `auto_wechat:ai_edit`；19000 复用既有 Local Agent 鉴权，禁止新增匿名本地接口。

## 12. 前端设计

### 12.1 小高素材库

- 私有素材、平台公共素材、回收站三个标签页。
- 现有全局导航、`AI小高剪辑` 标题栏和“素材库 / 剪辑工作台”切换保持不变，只重做其下方内容区。
- 桌面端采用左侧分类与标签、中间网格/列表、右侧预览详情三栏；移动端采用分类筛选抽屉、单栏素材区和全屏详情。
- 支持拖拽、多选文件、文件夹、搜索、分类、标签、五阶段状态、云端状态、时长和导入时间筛选。
- 素材项展示真实缩略图、时长、存储位置和最高优先级处理状态；详情展示五阶段进度、时间轴和人工确认结果。
- 支持批量分析、上传云端、分类/标签、删除、恢复和永久删除；平台公共素材对商户只读。
- 详情提供真实预览、场景/高光/可用/不稳定区间、重新分析、一键增稳、上传云端和用于剪辑。
- 公共素材只读，下载并校验后才能参与本地剪辑。

### 12.2 AI 小高剪辑

- 素材选择、平台模板、目标时长和增稳选项。
- 真实任务阶段、进度、取消和失败重试。
- 720P 草稿的片段、空镜、字幕和配乐轻量调整。
- 1080P 成片确认、预览、下载和主动上传云端。
- 不显示假任务、假素材、假统计或伪成功状态。

页面刷新后先从 9000 恢复业务状态，再向 19000 查询本地执行状态。浏览器与小高AI微信助手必须运行在同一台电脑。

## 13. 安全与错误处理

- 拒绝路径逃逸、符号链接、伪装扩展名和不可探测媒体。
- 资源不足在任务启动前阻断，不能边处理边耗尽磁盘。
- 日志只记录任务 ID、素材 ID、哈希前缀、阶段、进度、`failure_stage` 和稳定错误码。
- 原素材、转写原文、绝对路径、密钥和模型原始响应不得写入普通日志。
- 所有有效阶段产物必须钉住输入哈希、参数摘要、算法版本和媒体探测结果。
- 任一媒体质量门禁失败时，不得注册为可下载成片。

## 14. 复用决策

### 14.1 直接迁入候选

- `auto_edit` 的数据模型、时间边界、剪辑语法、严格解析和校验。
- 质量过滤、重复检测、字幕分段等纯逻辑及对应测试。

### 14.2 适配后迁入

- FunASR、场景分析、候选生成、FFmpeg 渲染、字幕和 BGM。
- BrollStudio 的 SHA-256、ffprobe、缩略图、代理视频和 Vid.Stab 自动增稳。
- 标签、稳定性评分和可用时间区间。

### 14.3 重写边界

- 9000 数据、权限、API、审计和 9100 代理。
- 19000 本地存储、队列、进程监管和恢复。
- React 素材库和轻量剪辑工作台。

### 14.4 延期

- ROI 跟踪增稳、PySide6、SQLite、固定豆包客户端、向量检索。
- `insert_broll`、`overlay_broll` 和完整多轨编辑器。

## 15. 验收

### 15.1 自动化

- 纯逻辑、模型、状态机、权限、商户隔离和迁移测试。
- 19000 路径安全、磁盘门禁、队列、取消和重启恢复测试。
- 使用合成媒体执行 Vid.Stab、裁切拼接、字幕、BGM 和双分辨率渲染。
- LLM、Embedding 和外部网络全部使用替身。
- 前端接口合同、离线状态、刷新恢复、TypeScript 和构建通过；9000 标准 `{success,data,message}` 响应经 `apiClient` 拦截器处理后只解包一层 `data`。
- 素材库批量导入合同通过：文件多选、双文件顺序流式导入、批次内素材 ID 唯一、单个失败继续和批次结束只刷新一次列表。
- Task 12 增强需补回收站查询、同商户 SHA-256 并发去重、五阶段状态、自动分析恢复、模型失败隔离、宝塔临时目录流式上传、人工确认不覆盖和跨端到期清理合同。
- 浏览器截图覆盖桌面三栏与移动单栏，断言既有导航、标题栏、模块切换和剪辑工作台未回归。

### 15.2 媒体强门

- 原素材 SHA-256 不变。
- 输出可被 ffprobe 读取且非空。
- 分辨率、时长、帧率、音频和时间戳符合任务合同。
- 字幕和口播不漂移，空镜不冻结尾帧。
- 增稳结果保持时长和音频，不覆盖原素材。

### 15.3 本地真实素材验收

在普通 Windows CPU 电脑上，用已授权的真实汽车口播素材完成完整单任务闭环。9100 使用替身；真实付费模型、生产数据库、宝塔和抖音发布统一留到 Phase 13 后的生产验证执行包。

### 15.4 甲方测试交付

检查点 C 通过后新增甲方测试专用单入口 EXE 交付。甲方只接收并启动 `小高AI系统测试版.exe`；外层控制进程使用 PyInstaller 自身临时目录承载 Local Agent 与 AI剪辑 Worker 两套隔离运行时，不额外维护 payload 清单或版本释放器。测试包只用于验证当前本地 MVP，不等同于生产验收；商户测试令牌不进入构建产物，Worker 不继承该凭据。按用户决定，许可证文件、组件清单、FFmpeg 构建信息和安全软件扫描均不作为 Task 11 开发、构建或测试交付的阻断条件。

## 16. 当前状态

- Phase 12 基础 MVP 需求、实现与执行包：Task 0-10 与 Task 10-FIX1~FIX6 已完成，检查点 A/B/C 均 PASS；历史基线状态为 `DONE_WITH_CONCERNS`，生产 concern 为 `baota_ai_edit_production_not_verified`。
- Phase 12 Task 12 素材库真实闭环增强：设计与执行包已批准；Task 12-1/12-2 提交链为 `bacc972 → 275ca0e`，当前 `CHECKPOINT_A_BLOCKED`。Task 12-2-FIX1 必须闭合 SQLite 迁移对象所有权与精确结构守卫、素材信任字段数据库约束、盘点工具只读边界、历史分析快照往返和 PostgreSQL 0015 静态合同测试，再回检查点 A 三方复审。
- Phase 12 当前总状态：Task 12 已进入数据合同实施阶段，但检查点 A 未放行；不得进入 Task 12-3，也不得把自动分析、云端上传、真实回收站和新素材库界面写成已完成。
- Phase 12 本地流程：前端→19000 创建/取消/重试→19000 调 9000 agent-create/agent-retry 登记 + Worker 协调；19000 导入/删除同步 9000 元数据；任务状态查询走 9000 权威源。§11 其余路由（`/jobs/{id}/plan`、`/confirm`、`/artifacts/{id}/preview`、`/download`、19000 `/jobs/{id}/start`）为一期已知缺口，不阻塞本地闭环。
- 甲方测试交付：Task 11 `BUILT_FOR_CUSTOMER_TEST`；单入口 EXE 已用 `https://merchant.xiaogaoai.cn/api`、前端 `https://merchant.xiaogaoai.cn/` 和商户 `m_nc_2bba00063cc13016` 重建，CArchive 配置、GET/DELETE CORS/PNA 预检、严格鉴权、单 token 商户绑定、9 路由、真实 Worker+FFmpeg、EasyOCR 预热与端口释放通过，SHA-256 为 `13A05B75EC3AF7E247EA86FDA26AA88AB2A81C9E4E23380EF4233B42C4BB6681`。旧包已在干净虚拟机暴露边界问题，新包仍需重新执行零安装复测。
- 正式客户安装包：`NOT_BUILT`；第三方分发依据未全部确认。
- 宝塔生产验证：`NOT_STARTED`，Phase 13 后统一执行。
- `auto_edit` 和 BrollStudio 外部仓库：继续只读，不作为运行时依赖。
