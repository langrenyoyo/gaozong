# Docker 部署专用依赖（Linux 兼容，排除 Windows 专用包 uiautomation/pyperclip/easyocr/torch）。
# 基于主依赖 requirements.txt，用 == 锁版本保证可复现；pymilvus 等已在主依赖中。
# 不含 Windows 专用 + OCR 重依赖（仅 Local Agent 本机用，见 requirements-windows.txt）。
-r requirements.txt

# 版本锁定（覆盖 requirements.txt 的 >= 浮动版本）
fastapi==0.136.3
python-multipart==0.0.32
uvicorn==0.49.0
sqlalchemy==2.0.50
asyncpg==0.31.0
alembic==1.17.2
pydantic==2.12.4
httpx==0.28.1
pymilvus==2.6.12
tos==2.9.2
volcenginesdkarkruntime>=1.0.0
