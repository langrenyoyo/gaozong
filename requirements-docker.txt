# Docker 部署专用依赖（Linux 兼容，排除 Windows 专用包）
# 版本锁定基于本地验证通过的环境
fastapi==0.136.3
uvicorn==0.49.0
sqlalchemy==2.0.50
asyncpg==0.31.0
psycopg[binary]>=3.1.0
alembic==1.17.2
pydantic==2.12.4
httpx==0.28.1
requests==2.32.3
pymilvus==2.6.12
# Phase 8 每日报表 Excel 导出（app/services/daily_report_excel.py 顶层依赖，缺失会启动即崩）
openpyxl>=3.1.5,<4
