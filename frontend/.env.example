# 前端环境变量示例
# 使用方式：复制为 .env.development（本地开发）或 .env.local（本地覆盖，禁止提交）。
# 注意：本文件只写示例 URL，严禁填写真实 token、API Key 或密钥。

# ---- auto_wechat 主后端（端口 9000）----
# client.ts 读取顺序：VITE_API_BASE_URL -> VITE_AUTO_WECHAT_API_BASE_URL -> DEV 兜底 127.0.0.1:9000
# Docker / 局域网开发默认走 Vite /api 代理，避免浏览器把 127.0.0.1 当成访问者电脑。
VITE_API_BASE_URL=/api
VITE_AUTO_WECHAT_API_BASE_URL=/api

# ---- 小高AI微信助手 Local Agent（端口 19000）----
# 19000 依赖 Windows 微信窗口、UIA、剪贴板、OCR 和本机 GUI 自动化，不进入 Docker。
# 需要微信能力时，请在 Windows 宿主机单独启动。
# 当前 localWechatAgent.ts 优先读取 localStorage("local_wechat_agent_url")，再兜底 127.0.0.1:19000；
# 保留此变量作为后续统一配置占位。
VITE_LOCAL_WECHAT_AGENT_BASE_URL=http://127.0.0.1:19000

# ---- 抖音AI小高客服（端口 9100）----
# 9100 已提供 RAG 知识库、LLM 回复建议和 /douyin-ai-cs-test 测试面板调用能力。
# Docker / 局域网开发默认走 Vite /ai-cs-api 代理，不让浏览器直连容器 service name。
VITE_DOUYIN_AI_CS_API_BASE_URL=/ai-cs-api

# ---- NewCarProject 外部登录服务（上游 8790）----
# 前端收到 ?code=xxx&source=new_car_project 后，必须由浏览器直连上游 exchange-code。
VITE_NEWCAR_AUTH_BASE_URL=http://192.168.110.19:8790
VITE_NEWCAR_LOGIN_URL=http://192.168.110.19:5174/login
