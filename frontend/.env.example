# 前端环境变量示例
# 使用方式：复制为 .env.development（本地开发）或 .env.local（本地覆盖，不提交）
# 注意：本文件只写示例 URL，严禁填写真实 token / API Key / 密钥

# ---- auto_wechat 主后端（端口 9000）----
# client.ts 读取顺序：VITE_API_BASE_URL → VITE_AUTO_WECHAT_API_BASE_URL → DEV 兜底 127.0.0.1:9000
VITE_API_BASE_URL=http://127.0.0.1:9000
VITE_AUTO_WECHAT_API_BASE_URL=http://127.0.0.1:9000

# ---- 小高AI微信助手 Local Agent（端口 19000）----
# 说明：当前 localWechatAgent.ts 通过 localStorage("local_wechat_agent_url") 读取，
#       并不直接读取本环境变量；保留此变量作为后续统一配置的预留位。
VITE_LOCAL_WECHAT_AGENT_BASE_URL=http://127.0.0.1:19000

# ---- 抖音AI小高客服（端口 9100，预留）----
# 说明：9100 客服 client 尚未实现，此变量为后续 douyinAiCsClient.ts 预留。
VITE_DOUYIN_AI_CS_API_BASE_URL=http://127.0.0.1:9100
