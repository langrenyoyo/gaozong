# 主依赖（平台无关核心包，本地/Docker 共用）。
# Windows 专用包（uiautomation/pyperclip/easyocr/torch/opencv）见 requirements-windows.txt。
# Docker 锁版本见 requirements-docker.txt（-r requirements.txt + == 覆盖）。
fastapi>=0.100.0
python-multipart>=0.0.6
uvicorn>=0.20.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg[binary]>=3.1.0
alembic>=1.13.0
pydantic>=2.0.0
httpx>=0.24.0
requests==2.32.3
Pillow>=10.0.0
openpyxl>=3.1.5,<4
pymilvus>=2.6.0
tos>=2.9.0
volcenginesdkarkruntime>=1.0.0
mutagen>=1.47.0
